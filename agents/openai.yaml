interface:
  display_name: "知识 A/B-roll 成片"
  short_description: "把 SRT、MP3 或现有 ChatCut 项目制作成 A/B-roll 知识视频"
  default_prompt: "使用 $knowledge-abroll-video，先盘点我的输入和已有素材，按阶段实际使用项目生产提示词，并按完整语义生成严格七列表头的 S001…视觉编排表；编排校验和必要确认完成后，再生成资产、组装 ChatCut 主时间线并由 ChatCut 导出，不能用新建项目跳过编排。"
