# 导演与视觉决策

## 1. 先理解，再切镜

先阅读全文，识别主题、章节、主张、解释、例子、转折、情绪和结论。镜头以完整语义为单位，边界只能选已有 SRT cue 的开始或结束时间。

每个语义段先回答：

1. 观众此刻要理解或感受到什么？
2. 静音看画面时，哪种关系能把这件事表达清楚？
3. 画面中什么会发生有效变化？
4. 镜头最终落在哪个可见结果上？
5. 下一镜继承的是人物、物体、方向、颜色还是意义？

没有具体知识增量的铺垫、态度、经历或感叹优先 A-roll；涉及知识、概念、信息增量、结构、关系、步骤、对比、证据或具体对象时使用 B-roll。不要为了形式上的 A/B 交替破坏语义，但连续三镜以上同类画面必须改变视角或结构并写明理由。

## 2. 主画面 A

A 画面承担讲述者、情境和情绪。

固定人物模式为每镜标记一个 `a_view`：`presenter`、`protagonist`、`supporting` 或 `first-person`。这是构图视角，不是四套角色或四种画风。

### 固定人物微场景

- 一个固定人物或 IP 贯穿全片；
- 人物通过动作、姿态、表情、道具和环境关系表达内容；
- 先锁定五官、头身比例、体型、服饰、配色、线条和阴影规则；
- 只有一张人物参考图时，先做三视图和细节规范，再生成镜头资产；
- 主持人、主角、配角和第一人称只是构图视角，不是四套人物风格。

### 完整 AI 场景

- 每个镜头是一幅完整场景，不要求固定主角；
- 全片锁定配色、材质、线条、光线、人物表现和场景复杂度；
- 重复人物或标志性物体仍需使用同一规范和参考资产。

### 封闭构图

- 主体居中或视觉重心稳定，动作和因果关系在画布内闭合；
- 关键人物、手脚、道具、符号、运动线和环境线完整收在画布内；
- 默认让所有有效绘制痕迹距资产边缘至少 8%，除非镜头规范另有经过验证的安全值；
- 背景提供必要情境和层次，不抢主体，不依赖画外空间才能理解；
- 给轻微推拉、平移和外部字幕留余量；不同画幅分别出图或重新构图，不硬裁。

长 A 镜头不能只做循环呼吸。按旁白安排发现、犹豫、选择、行动或结果等状态变化；一张静态图只承载一个原子动作与其结果。

## 3. 信息画面 B

B 画面承担概念、定义、因果、流程、层级、组成、对比、证据和真实对象。

子类型：

- `verified-media`：用户提供或已经核实的截图、录屏、图片、数据和案例；
- `diagram`：卡片、关系图、流程图、图标和可视化结构；
- `text-only`：章节、金句或结论，克制使用。

B 是全屏舞台，不是覆盖人物的浮层。动画只保留语义变化：元素出现、重点选择、关系建立、方案比较、步骤推进和结论落定。背景循环、装饰粒子、单纯淡入和字幕出现不算有效信息变化。

默认使用独立深色知识舞台强化与明亮手绘 A-roll 的职责差异。`verified-media` 保留素材原色并置于统一舞台和标注系统中；`diagram` 与 `text-only` 默认使用深色背景。用户选择其他配色时，必须在基线阶段验证 A/B 仍能快速区分。

## 4. 信息动效模板

先用语义结构、信息项数量、可用时长和画幅运行 `scripts/select_broll_template.py` 搜索本地模板。常见结构为：

- 对比、聚合、筛选、层级、因果；
- 替换、展开、流程、时间线、组成关系。

模板命中时只替换内容和必要视觉 token。没有合格命中时，必须先检查语义目录中的外部候选，不得以“从零实现更快”为由跳过。逐镜读取具体镜头卡；候选为 `port-required` 时继续读取对应实现文件，再提取元素关系、主要动作和阶段顺序。候选均不适合时，逐项说明拒绝理由并记录吸收的运动原则，才允许新建模板。公开可见不等于允许复制：未检测到许可证时只做抽象结构研究。

详细记录格式和验证命令见 [B-roll 外部骨架研究门](broll-external-research.md)。静态 SVG 可以作为动画组件，不能单独证明信息动效已经完成。

每个模板必须记录 ID、语义结构、适用内容、信息项范围、时长范围、画幅、字段、动画阶段、预览、技术来源、许可证、移植状态、验证状态和已知限制。

## 5. 视觉编排字段写法

每个镜头至少写清：

- 镜头编号、开始与结束时间、覆盖的 SRT cue；
- 原字幕和观众要理解的一句话；
- A/B 职责及子类型；
- 主体、构图、景别、动作、表情、道具或信息元素；
- 按旁白顺序发生的有效变化与最终画面；
- 与前后镜头的视觉或意义衔接；
- 所需素材、来源、真实性风险、制作难度；
- 模板 ID 或新模板需求。

原字幕保持逐字一致。真实素材缺失时写 `needs-supplied-material`，不得用生成内容冒充。

## 6. 全片复查

- 每个镜头是否明确帮助理解或感受？
- A-roll 是否在讲人，B-roll 是否在讲知识或信息增量？
- 是否连续重复同一职责、构图、视角或动效结构？
- 是否有长时间无有效变化或短时间信息过载？
- 是否提前表现后文结论，或继承了无意义的上一镜道具？
- A/B 切换是否由语义驱动，并有方向、颜色、物体或意义上的承接？
- 是否存在未经文案授权的推断、数据、产品细节或案例？
- 所有文字是否能在目标画幅和可用时长内读清？
